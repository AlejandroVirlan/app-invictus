/* Clase Usuario que guarda el nombre de usuario
 * y el password con un constructor que inicializa ambos
 * atributos.
 */
export class Usuario {
    id: number;
    username: string;
    password: string;
    email: string;
    enabled: boolean;
    creado: string;

    constructor(){
        
    }
}
